/* block comments can go on multiple lines BUT THEY SHOULD BE AVOIDED,
because sometimes this syntax appears in regex literals */

// inline comments can not. They are safer.

const that = this;
console.log(that);

